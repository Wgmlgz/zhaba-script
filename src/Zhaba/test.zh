use 'frog.zh'
use 'out.zh'
use 'range.zh'

fn bool_test str msg bool b
  ? b: <msg<': true'<
  \ <msg<': false'<

fn main
  a := 5i64
  b := 0i32
  c := 0i64
  < a < b < c < 
  s := 'aboba'
  > a < a <

  @ i 0..100000
    < i < 
  // < 'Enter a, b, c:' < > a > b > c < a < b < c <
