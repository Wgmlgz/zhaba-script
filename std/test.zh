use vec
use range
use out
use frog
use str

fn prikol Vec<int>R v
  out sizeof(v)
  v.at(0) = 5

fn prikol_copy Vec<int> v
  out sizeof(v)
  v.at(0) = 2

fn prikol intR t
  t = 4

fn main
  v := Vec<int>(4)
  v += iota(-5 0)
  // n := v.sub(2 5)
  prikol(v)
  v += 1
  v += 2
  v += 3
  a := v
  v += v
  v = a
  prikol_copy(v)
  < v <
  sort(v)  
  < v <
  v.free_mem()
  < v <
  
  s := Str('aboba')
  < s <
  // n.print()

  // v.push_back(54)
  // v.push_back(54)
  // v.push_back(54)
  // v.push_back(54)
  // v.print()
  // n.print()

  // out (&v as int)
  // out (&n as int)
  // @ i v
  //   out (*i)