use 'tuple.zh'

fn sleep int n
  #unsigned int retTime = clock() + n;
  #while (clock() < retTime);

fn cls
  #'system("cls")'

fn u64 rng
  #static unsigned long long x = 666
  #x ^= x << 13
  #x ^= x >> 7
  #x ^= x << 17
  #return x
  
fn swap intP a intP b
  int t = *a
  *a = *b
  *b = t

fn char asch str s: <<< *(s as charP)

impl Arg1
  fn swap T1P a T1P b 
    auto t = *a
    *a = *b
    *b = t

  fn int sizeof
    T1 val
    #'return sizeof(val)'
