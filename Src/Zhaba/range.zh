type range
  int begin end cur step uninit
  
int ++ __ rangeP r:
  ? (*r).uninit: (*r).uninit = 0
  \ (*r).cur = (*r).cur + (*r).step
  ? (*r).begin < (*r).end: <<< (*r).cur < (*r).end
  \ <<< (*r).cur > (*r).end

7 range .. ___ int begin int end
  range r
  r.begin = begin
  r.end = end
  r.step = 1
  r.uninit = 1
  r.cur = begin
  <<< r

7 range .. ___ range r int step
  r.step = step
  <<< r
